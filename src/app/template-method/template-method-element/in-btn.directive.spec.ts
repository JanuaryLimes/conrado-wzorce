import { InBtnDirective } from './in-btn.directive';

describe('InBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new InBtnDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
